#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin>>n;
    vector<int> x(n+1);
    vector<int> h(n+1);
    for (int i = 1; i<=n; i++) {
        cin>>x[i]>>h[i];
    }

    








    return 0;
}